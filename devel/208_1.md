# 任务 208_1: 实现 (liii unicode) 模块

## 任务描述
实现 `(liii unicode)` 模块，统一导出已有的 Unicode 相关函数，并将测试用例迁移到专门的测试文件中。

## 目标
1. 创建 `(liii unicode)` 模块
2. 导出现有的 Unicode 相关函数
3. 迁移相关测试用例到 `unicode-test.scm`

## 需要导出的现有函数

### 从 (liii base) 导出的 UTF-8 函数
- `utf8->string` - UTF-8 字节向量转字符串
- `string->utf8` - 字符串转 UTF-8 字节向量
- `u8-string-length` - UTF-8 字符串长度
- `u8-substring` - UTF-8 子字符串
- `bytevector-advance-u8` - UTF-8 字节向量前进

### Unicode 常量
- `unicode-max-codepoint` - Unicode 最大码点
- `unicode-replacement-char` - Unicode 替换字符

## 需要迁移的测试用例

### 从 lang-test.scm 迁移的测试
- Unicode 字符分类测试
- Unicode 数字字符测试
- Unicode 大小写转换测试
- Unicode 字符串操作测试
- UTF-8 编码/解码测试

## 实现步骤

1. 检查并完善 `goldfish/liii/unicode.scm` 模块文件
2. 在模块中重新导出现有的 Unicode 相关函数
3. 添加 Unicode 字符包装函数和常量
4. 创建 `tests/goldfish/liii/unicode-test.scm` 测试文件
5. 从现有测试文件中迁移 Unicode 相关测试用例
6. 确保所有测试通过

## 注意事项
- 保持与现有函数的兼容性
- 确保重新导出的函数行为一致
- 测试用例迁移后需要验证正确性
- 遵循项目的代码格式规范

## 完成状态

✅ **已完成**

1. ✅ 检查并完善 `goldfish/liii/unicode.scm` 模块文件
   - 模块已存在且功能完整
   - 导出了所有 UTF-8 相关函数
   - 添加了 Unicode 字符包装函数
   - 定义了 Unicode 常量

2. ✅ 创建 `tests/goldfish/liii/unicode-test.scm` 测试文件
   - 包含 221 个测试用例
   - 覆盖所有 Unicode 相关功能

3. ✅ 从现有测试文件中迁移 Unicode 相关测试用例
   - 从 `lang-test.scm` 迁移了所有 Unicode 相关测试
   - 包括 UTF-8 编码/解码、字符分类、大小写转换等测试

4. ✅ 确保所有测试通过
   - 所有 221 个测试用例都成功通过
   - 功能验证完整

### 实际导出的函数

**UTF-8 函数（从 (liii base) 重新导出）：**
- `utf8->string`, `string->utf8`, `u8-string-length`, `u8-substring`, `bytevector-advance-u8`

**Unicode 字符包装函数：**
- `unicode-char-ascii?`, `unicode-char-numeric?`, `unicode-char-digit?`
- `unicode-char-upper?`, `unicode-char-lower?`
- `unicode-char-to-upper`, `unicode-char-to-lower`

**Unicode 常量：**
- `unicode-max-codepoint` (#x10FFFF)
- `unicode-replacement-char` (#xFFFD)