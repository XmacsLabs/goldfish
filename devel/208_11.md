# [208_11] 将 u8-string-length 改为 utf8-string-length

## 任务相关的代码文件
- goldfish/liii/unicode.scm
- tests/goldfish/liii/unicode-test.scm

## 如何测试
```
xmake config --yes
xmake b goldfish
bin/goldfish tools/lint.scm goldfish/liii/unicode.scm
bin/goldfish tools/lint.scm tests/goldfish/liii/unicode-test.scm
bin/goldfish tests/goldfish/liii/unicode-test.scm
```

## 2025-10-20 将 u8-string-length 改为 utf8-string-length

### What
将函数名 `u8-string-length` 重命名为 `utf8-string-length`，使其命名更加清晰和一致。

1. 在 `goldfish/liii/unicode.scm` 中修改函数定义
2. 在 `tests/goldfish/liii/unicode-test.scm` 中更新测试用例
3. 验证重命名后功能正常

### Why
- 使函数命名更加明确，`utf8-string-length` 比 `u8-string-length` 更清楚地表明这是处理 UTF-8 编码的函数
- 保持与项目中其他 UTF-8 相关函数命名的一致性
- 提高代码的可读性和维护性

### How
1. 使用全局搜索和替换的方式确保所有引用都被正确更新
2. 运行测试确保重命名后功能没有破坏
3. 检查代码格式和语法正确性