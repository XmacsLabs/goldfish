(define-library (scheme core)
  (export (rename open-binary-output-file open-binary-output-file)
          (rename open-binary-input-file open-binary-input-file)
          (rename digit-value digit-value)
          (rename symbol->string symbol->string)
          (rename string->symbol string->symbol)
          (rename symbol? symbol?)
          (rename assoc assoc)
          (rename assv assv)
          (rename assq assq)
          (rename member member)
          (rename memv memv)
          (rename memq memq)
          (rename list-set! list-set!)
          (rename list-ref list-ref)
          (rename list-tail list-tail)
          (rename reverse reverse)
          (rename append append)
          (rename length length)
          (rename list list)
          (rename make-list make-list)
          (rename list? list?)
          (rename null? null?)
          (rename cddr cddr)
          (rename cdar cdar)
          (rename cadr cadr)
          (rename caar caar)
          (rename set-cdr! set-cdr!)
          (rename set-car! set-car!)
          (rename cdr cdr)
          (rename car car)
          (rename cons cons)
          (rename pair? pair?)
          (rename string->number string->number)
          (rename number->string number->string)
          (rename denominator denominator)
          (rename numerator numerator)
          (rename gcd gcd))
  (begin))
